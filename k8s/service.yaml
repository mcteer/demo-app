apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: my-ns-dev
  labels:
    app: my-service
spec:
  type: ClusterIP
  selector:
    app: my-service
  ports:
    - port: 80
      targetPort: 8080
